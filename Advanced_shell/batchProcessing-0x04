#!/bin/bash

# Array of Pokémon to fetch
pokemon=("pikachu" "bulbasaur" "charmander")

for p in "${pokemon[@]}"; do
    curl -s "https://pokeapi.co/api/v2/pokemon/$p" -o "$p.json" &
done

# Wait for all background processes to finish
wait

echo "All Pokémon data fetched."
